# Rust
/target
**/target
Cargo.lock

# Editor
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# CLI test output (frames + verbose logs may contain user screen content
# including secrets) — never commit.
/frames
/frames/
*.log
log.txt
preview.log
demo.mp4
*.png

# Training dataset images may contain user screen content — never
# commit. Only the directory structure (.gitkeep) is tracked.
/dataset/images/
/dataset/labels/
/training/runs/
*.pt
*.onnx

# Tauri build artefacts (added in Phase 3)
src-tauri/target/
src-tauri/WixTools/
